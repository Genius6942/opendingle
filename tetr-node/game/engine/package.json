{
  "name": "tortise",
  "version": "1.0.0",
  "description": "a slow af tetris engine meant to be somewhat compatible with tetr.io",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --outDir build/full --declaration --emitDeclarationOnly index.ts && rollup -c && rm -rf build/full"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/plugin-typescript": "^11.1.6",
    "rollup": "^4.9.5",
    "rollup-plugin-dts": "^6.1.0"
  }
}
